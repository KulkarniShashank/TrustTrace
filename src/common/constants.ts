export enum CREDEBLEndpoints {
  ORG_PREFIX = `/orgs/`,
}

export enum CommonConstants {
  millisecondToSecond = 1000,
  minusTokenExpiryTimeInSeconds = 120,
  CREDEBL_ACCESS_TOKEN_KEY = 'CREDEBL_ACCESS_TOKEN_KEY',
}
